# =============================================================================
# ENVIRONMENT VARIABLES EXAMPLE
# =============================================================================
# Copy this file to .env and fill in your actual values
# Never commit .env to version control!

# =============================================================================
# GITHUB INTEGRATION
# =============================================================================
# GitHub Personal Access Token for fetching organization data
# Required for: /projects page, member data, repository information
# How to get: GitHub Settings > Developer settings > Personal access tokens
# Permissions needed: public_repo, read:org, read:user
GITHUB_TOKEN=your_github_personal_access_token_here

# Alternative public token (optional, for client-side requests)
# NEXT_PUBLIC_GITHUB_TOKEN=your_public_github_token_here

# =============================================================================
# ADMIN AUTHENTICATION
# =============================================================================
# Admin login credentials for the admin panel
# Used for: /admin/login, student management, attendance tracking
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=your_secure_admin_password_here

# JWT Secret for token signing and verification
# Generate a secure random string (recommended: 64+ characters)
# You can use: openssl rand -base64 64
JWT_SECRET=your_jwt_secret_key_here_make_it_long_and_secure

# =============================================================================
# DATABASE
# =============================================================================
# MongoDB connection string
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/database
# For local MongoDB: mongodb://localhost:27017/your_database_name
MONGODB_URI=mongodb://localhost:27017/pgs_software_club

# =============================================================================
# NEXT.JS ENVIRONMENT
# =============================================================================
# Automatically set by Next.js, but you can override if needed
# NODE_ENV=development

# =============================================================================
# OPTIONAL: EXTERNAL SERVICES
# =============================================================================
# Add any additional service API keys here as your app grows
# Examples:
# STRIPE_SECRET_KEY=sk_test_...
# SENDGRID_API_KEY=SG...
# CLOUDINARY_URL=cloudinary://...

# =============================================================================
# DEVELOPMENT NOTES
# =============================================================================
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in all required values above
# 3. Never commit .env to git (it's in .gitignore)
# 4. For production, set these in your hosting platform's environment variables
# 5. Restart your development server after changing environment variables